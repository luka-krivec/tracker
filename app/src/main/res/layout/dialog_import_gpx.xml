<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="25dp"
    tools:context=".ImportGpxActivity" >

    <EditText
        android:id="@+id/editFileGpxImport"
        android:layout_width="200dp"
        android:layout_height="wrap_content"
        android:layout_marginRight="10dp"
        android:hint="@string/hint_browse_gpx"
        android:editable="false"/>

    <ImageButton
        android:id="@+id/btnFileBrowse"
        android:src="@drawable/button_browse"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_toRightOf="@id/editFileGpxImport"
        android:contentDescription="@string/btn_browse"
        android:background="@android:color/transparent" />

</RelativeLayout>
